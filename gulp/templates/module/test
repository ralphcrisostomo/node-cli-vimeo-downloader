'use strict'

Module = require('../../../app/modules/<%= name.capitalize() %>')

describe 'Module `<%= name.capitalize() %>`', ->

  beforeEach ->
    @module = new Module()

  afterEach ->
    @module = null

  it 'should be an instance', ->
    expect(@module).to.be.an.instanceof(Module)